<h1><%= @room.name %></h1>
<hr>
<%= link_to 'New Custom Price', new_room_custom_price_path(@room) %>

<h2>Custom Prices</h2>

<% if @custom_prices.any?%>
  <% @custom_prices.each do |cp| %>
    <div>
      <dl>
        <strong><dt>Daily Price:</dt></strong>
        <dd>           
          De <%= cp.start_date %> até <%= cp.end_date %>
        </dd>
        <dd>
          $<%= cp.daily_price %>
        </dd>
      </dl>
    </div>
  <% end %>
<% else %>
  <p>There is no custom price</p>
<% end %>