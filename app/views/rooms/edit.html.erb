<h1>Edit Room</h1>

<% if @room.errors.any? %>
  <p>Check the errors below:</p>
  <ul>
    <% @room.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>